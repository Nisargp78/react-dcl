import React from "react";
import ReactDOM from "react-dom/client";

let root = ReactDOM.createRoot(document.getElementById("root"));

// let div = React.createElement("div", { id: "demo" }, React.createElement("header", { className: "demo" }, "header"), React.createElement("section", { className: "section" }, "sec1"), React.createElement("section", { className: "demo" }, "sec2"), React.createElement("footer", { className: "demo" }, "footer"));

let header = <header>Header</header>

root.render(div);